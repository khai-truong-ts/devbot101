services:
  - type: web
    name: devbot101
    runtime: docker
    envVars:
      - key: SLACK_APP_TOKEN
        sync: false
      - key: SLACK_BOT_TOKEN
        sync: false
      - key: CLAUDE_API_KEY
        sync: false
